import React from 'react';
import PageFooter from '.';

export default {
    title: 'Molecules/PageFooter',
    component: PageFooter
}

const Template = (args: any) => <PageFooter {...args} />;

export const Default = Template.bind({});
